# Copia in .env.local e su Vercel (Production).
# Connection string Postgres di Supabase: pooler Transaction (porta 6543).
# Non committare mai la password.

DATABASE_URL=

# FlixPatrol API v2: Basic auth (la chiave è lo username, password vuota).
# Serve per le Top 10 in-app di Netflix, Disney+, Prime, Max e gli altri cataloghi italiani.
FLIXPATROL_API_KEY=
